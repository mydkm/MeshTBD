# Examples

Large demo meshes are now treated as local-only assets instead of tracked repository content.

If you want to run the demo workflow:

1. Place your meshes in `local_data/demo/`
2. Run:
```bash
bash scripts/demo/run_demo.sh
```

The script writes outputs to `local_outputs/demo/`.
