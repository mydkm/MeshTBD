# Demo

Attached are the 3D mesh files currently being used to demo MeshTBD!

To run the demo, just enter the following command into your terminal from the repository's root directory:
```bash
bash demoScript.sh
```